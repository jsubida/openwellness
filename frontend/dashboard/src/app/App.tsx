// Placeholder app shell — replaced by the RouterProvider once the auth
// feature lands (see src/app/router.tsx in the feature plan).
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-6">
      <h1 className="text-4xl font-bold text-primary">OpenWellness</h1>
    </main>
  )
}
