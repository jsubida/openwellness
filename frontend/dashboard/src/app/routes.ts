// Centralized route paths. Keep these as the single source of truth so the
// router, redirects, and navigations never drift out of sync.
export const ROUTES = { landing: '/', home: '/home' } as const
