package edu.openwellness.mobile.core.presentation.theme

import androidx.compose.material3.Typography

// Default M3 type scale (platform default typeface). Centralized here so a
// branded typeface can be slotted in later without touching call sites.
val OpenWellnessTypography = Typography()
